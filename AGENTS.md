# 项目协作规则

## 更新日志

- 每次新增、修改或删除代码、场景逻辑、项目配置时，必须同步更新项目根目录的 `CHANGELOG.md`。
- 在 `CHANGELOG.md` 顶部新增版本记录，包含版本号、当天日期、变更分类和具体修改内容。
- 变更分类使用 `新增`、`调整`、`修复`；一项修改涉及多个分类时分别记录。
- 日志必须准确描述本次实际完成的改动，不得记录尚未实现或未经验证的内容。
- 只有代码改动、必要验证和更新日志均已完成，任务才算完成。
- `AGENTS.md`、`CHANGELOG.md` 以及其他中文文档统一使用 UTF-8 编码。
